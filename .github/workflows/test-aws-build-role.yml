name: Test BUILD_ROLE with tf-checks
permissions:
  id-token: write
on:
  push:
    branches:
      - 'test/build_role'
jobs:
  tf-checks-build-role:
    uses: ./.github/workflows/tf-checks.yml
    with:
      working_directory: './examples/complete/'
      provider: aws
      enable_plan: true
      show_plan: false
      terraform_version: '1.13.0'
    secrets:
      BUILD_ROLE: ${{ secrets.BUILD_ROLE }}